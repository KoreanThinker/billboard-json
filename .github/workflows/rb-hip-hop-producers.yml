name: R&B Hip-Hop Producers

on:
  workflow_dispatch:
  schedule:
    - cron: '11,41 * * * *'

jobs:
  cron:
    strategy:
      matrix:
        platform: [ubuntu-latest]
        node: ["16"]
    name: Node ${{ matrix.node }} (${{ matrix.platform }})
    runs-on: ${{ matrix.platform }}
    defaults:
      run:
        working-directory: crawler

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}

      - name: Install modules
        run: yarn

      - name: Run crawling
        run: yarn rb-hip-hop-producers
      
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"

      - name: Add & Commit
        uses: EndBug/add-and-commit@v9.0.0
        with:
          author_name: R&B Hip-Hop Producers Crawler
          message: 'R&B Hip-Hop Producers crawled at ${{ steps.date.outputs.date }}'
